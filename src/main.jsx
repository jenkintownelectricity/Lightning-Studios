import React from 'react'
import ReactDOM from 'react-dom/client'
import LightningStudio from './LightningStudio.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LightningStudio />
  </React.StrictMode>,
)
